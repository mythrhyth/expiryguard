export enum Role {
  ADMIN = "ADMIN",
  MANAGER = "MANAGER",
  VIEWER = "VIEWER"
}

export enum Priority {
  HIGH = "HIGH",
  MEDIUM = "MEDIUM",
  LOW = "LOW"
}

export enum Severity {
  INFO = "INFO",
  WARNING = "WARNING",
  DANGER = "DANGER",
  CRITICAL = "CRITICAL"
}
